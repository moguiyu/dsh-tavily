{
  "name": "dsh-tavily-workspace",
  "version": "0.2.0",
  "private": true,
  "description": "Tavily web_search provider for DSH with extract, map, crawl, key rotation, usage gauge, and settings.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "dependencies": {
    "@moguiyu/dsh-tavily": "^0.2.0"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "license": "MIT"
}
